import 'dart:ui';

class TargetNode {
  final int id;
  final String letter;
  final Color color;
  final double angle;
  final String sound;
  final String text;
  TargetNode(this.id, this.letter, this.color, this.angle, this.sound, this.text);
}
